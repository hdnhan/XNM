mesh Th = square(4, 4);
// plot(Th);
for(int i = 0; i < Th.nv; i++){
    int it = Th(Th(i).x, Th(i).y).nuTriangle;
    for (int j = 0; j < 3; j++){
        if (Th(i).x == Th[it][j].x && Th(i).y == Th[it][j].y){
            cout << "i = " << i << ": " << Th[it][j] << endl;
        }
    }
}